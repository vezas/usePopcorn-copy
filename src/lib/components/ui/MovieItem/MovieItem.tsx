import { FC } from 'react';

interface MovieItemProps {
  movie: [];
}

export const MovieItem: FC<MovieItemProps> = () => {
  return (
    <div>
      <img src='' alt='' />
    </div>
  );
};
